# Day-engine orchestration (tick, briefing, wrapup, proactive nudges) is
# ported in a later rework stage. This package currently holds only the
# data layer that agent/tools already depends on: state.py (day_tasks/
# recurring_tasks CRUD), pool.py (untimed backlog CRUD), research.py
# (web research helper for tasks).
